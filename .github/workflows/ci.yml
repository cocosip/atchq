name: CI

on:
  push:
    branches: [master]
  pull_request:
    branches: [master]
  workflow_dispatch:

jobs:
  verify:
    name: Build, test and quality gates (${{ matrix.os }})
    strategy:
      # run every OS to the end so one failing platform cannot hide the other's result
      fail-fast: false
      matrix:
        os: [ubuntu-latest, windows-latest]
    runs-on: ${{ matrix.os }}
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up JDK 21
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '21'
          cache: maven

      - name: Build, test and verify (tests + jacoco + spotless)
        # bash works on both runners: .gitattributes keeps mvnw LF-ended, so the
        # wrapper script also runs under Git Bash on Windows
        # the surefire argLine in the root pom already carries the JDK 21 JVM opens
        # required by Chronicle; spotless:check and jacoco are bound to verify
        shell: bash
        run: ./mvnw --batch-mode verify

      - name: Upload test reports on failure
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          # artifact names must be unique across matrix jobs
          name: test-reports-${{ matrix.os }}
          path: '**/target/surefire-reports/**'
          retention-days: 7

      - name: Upload jacoco coverage
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: jacoco-report-${{ matrix.os }}
          path: '**/target/site/jacoco/**'
          retention-days: 7

  benchmarks:
    name: JMH benchmarks (manual trigger only)
    runs-on: ubuntu-latest
    needs: verify
    if: github.event_name == 'workflow_dispatch'
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up JDK 21
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '21'
          cache: maven

      - name: Run benchmarks
        # the benchmark main method appends the JDK 21 JVM opens to the forked JMH JVM itself
        run: |
          ./mvnw --batch-mode -pl latchq-benchmarks -am package
          java -jar latchq-benchmarks/target/latchq-benchmarks-*.jar

      - name: Upload benchmark results
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: benchmark-results
          path: '**/target/benchmark-data/**'
          retention-days: 14
